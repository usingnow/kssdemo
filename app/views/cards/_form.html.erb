<%= simple_form_for(@card) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :number %>
    <%= f.input :status %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
