<%= simple_form_for(@identity) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :password %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
